LoadPlugin "network"

<Plugin "network">
    <% if scope.lookupvar('server') != :undef %>
        Server "<%= @server %>" "<%= @port %>"
    <% else %>
        Listen "127.0.0.1" "<%= @port %>"
        Listen "<%= @listen_ip %>" "<%= @port %>"
        ReportStats true
    <% end %>
</Plugin>

